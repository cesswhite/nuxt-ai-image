/**
 * OpenAI text helpers for the studio (enhance / surprise). Keep in sync with `server/api/text/*.post.ts`.
 */
export const TEXT_API_ENHANCE_PROMPT = '/api/text/enhance-prompt' as const
export const TEXT_API_SURPRISE_PROMPT = '/api/text/surprise-prompt' as const
